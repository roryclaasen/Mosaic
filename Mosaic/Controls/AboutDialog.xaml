<?xml version="1.0" encoding="utf-8"?>
<ContentDialog
    x:Class="Mosaic.Controls.AboutDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Style="{StaticResource DefaultContentDialogStyle}"
    x:Uid="AboutDialog"
    DefaultButton="Close"
    PrimaryButtonClick="ContentDialog_PrimaryButtonClick"
    >
    <StackPanel Orientation="Vertical">
        <TextBlock IsTextSelectionEnabled="True">
            <Run Text="{x:Bind ApplicationDisplayName}" />
            <LineBreak />
            <Run x:Uid="AboutDialog_VersionLabel" />
            <Run Text="{x:Bind ApplicationVersion}" />
        </TextBlock>

        <HyperlinkButton x:Uid="AboutDialog_SourceCodeLink" NavigateUri="https://github.com/roryclaasen/Mosaic" />
    </StackPanel>
</ContentDialog>
